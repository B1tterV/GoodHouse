export interface CatalogNavCard {
    id: number;
    icon?: string;
    iconComponent?: string;
    text: string;
    slug: string
}

export interface SubcategoryItem {
    id: number;
    image?: string;
    name: string;
    slug: string
}
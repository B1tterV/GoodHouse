export interface CatalogNavCard {
    id: number;
    icon?: string;
    image?: string;
    name: string;
    slug: string
}

export interface SubcategoryItem {
    id: number;
    image?: string;
    name: string;
    slug: string
}